apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: p-pluto
  namespace: argocd
spec:
  destinations:
    - name: aks-ninja-dev-06
      namespace: ns-pluto
  roles:
    - description: Admin privileges to namespace
      groups:
        - e560a3b9-df9f-4ed8-80c7-a6a517d84b91
      name: admin
      policies:
        - p, proj:p-pluto:admin, applications, create, p-pluto/*, allow
        - p, proj:p-pluto:admin, applications, delete, p-pluto/*, allow
        - p, proj:p-pluto:admin, applications, get, p-pluto/*, allow
        - p, proj:p-pluto:admin, applications, override, p-pluto/*, allow
        - p, proj:p-pluto:admin, applications, sync, p-pluto/*, allow
        - p, proj:p-pluto:admin, applications, update, p-pluto/*, allow
        - p, proj:p-pluto:admin, applications, action/*, p-pluto/*, allow
        - p, proj:p-pluto:admin, logs, get, p-pluto/*, allow
        - p, proj:p-pluto:admin, exec, create, p-pluto/*, allow
        - p, proj:p-pluto:admin, projects, get, p-pluto, allow
        - p, proj:p-pluto:admin, repositories, get, p-pluto/*, allow
        - p, proj:p-pluto:admin, repositories, create, p-pluto/*, allow
        - p, proj:p-pluto:admin, repositories, update, p-pluto/*, allow
        - p, proj:p-pluto:admin, repositories, delete, p-pluto/*, allow
  sourceNamespaces:
    - ns-pluto
  sourceRepos:
    - 'https://github.com/jonasnorlund/aks-platform-tenants'
  # Deny all cluster-scoped resources from being created, except for Namespace
  clusterResourceWhitelist:
    - group: ''
      kind: Namespace
  # Allow all namespaced-scoped resources to be created, except for ResourceQuota, LimitRange, NetworkPolicy
  namespaceResourceBlacklist:
    - group: ''
      kind: ResourceQuota
    - group: ''
      kind: LimitRange
    - group: ''
      kind: NetworkPolicy
